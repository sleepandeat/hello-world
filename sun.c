#include<cstdio.h>
#include<math.h>
{
	int n,h[111],width,height,sum,i,di,num,level,id,cases,dh,dw,h1,m1,h2,m2;
	//
	double min,max,angle,Half_pi;
	//
	int main()
	{
		Half_pi=acos(0);
		while(scanf(""))
	}
}